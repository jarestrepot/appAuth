import { ColorIconDirectiveDirective } from './color-icon-directive.directive';

describe('ColorIconDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorIconDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
